Options -MultiViews

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Se for um arquivo ou diretório real, serve normalmente
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Senão, redireciona **tudo** para index.html (rota client-side)
  RewriteRule ^ index.html [L]
</IfModule>
